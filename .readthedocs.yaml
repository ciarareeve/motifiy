# .readthedocs.yaml
version: 2

# Build documentation in the docs/ directory with Sphinx
sphinx:
  configuration: docs/source/conf.py

# Optionally set the version of Python and other tools you use
python:
  version: 3.10
  install:
    - requirements: requirements.txt

